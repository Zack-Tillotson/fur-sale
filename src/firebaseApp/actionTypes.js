const namespace = 'firebaseApp/';

const apiSuccessful = namespace + 'apiSuccessful';
const apiFailed = namespace + 'apiFailed';
const dataReceived = namespace + 'dataReceived';
const ui = namespace + 'ui';

export default {
  apiSuccessful,
  apiFailed,
  dataReceived,
  ui,
}